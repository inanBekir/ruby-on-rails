<%= form_with(model: note, local: true) do |form| %>
  <% if note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(note.errors.count, "error") %> prohibited this note from being saved:</h2>

      <ul>
        <% note.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :"Note Header" %>
    <%= form.text_field :nHeader %>
  </div>

  <div class="field">
    <%= form.label :"Note Content" %>
    <%= form.text_field :nContent %>
  </div>

  <div class="field">
  <%= form.hidden_field :ownerId, :value => current_user.id%>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
